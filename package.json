{
  "name": "ai-operations",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7"
  },
  "lint-staged": {
    "frontend/**/*.{ts,tsx}": [
      "pnpm -C frontend exec eslint --fix --no-warn-ignored"
    ],
    "backend/src/**/*.{js,ts}": [
      "pnpm -C backend exec eslint --fix --no-warn-ignored"
    ]
  }
}
