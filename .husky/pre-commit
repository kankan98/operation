#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

set -e

# Run package-local lint-staged checks for both independent projects.
pnpm exec lint-staged --cwd backend
pnpm exec lint-staged --cwd frontend

