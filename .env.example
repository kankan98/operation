# Local data foundation runtime only.
# Do not put production credentials in this file.
DATABASE_URL=postgres://operation:operation_dev_password@127.0.0.1:5433/operation_dev

# Optional local DeepSeek provider smoke check.
# Keep real keys in .env only; never commit or print them.
DEEPSEEK_API_KEY=
DEEPSEEK_API_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-pro
DEEPSEEK_TIMEOUT_MS=30000
DEEPSEEK_MAX_TOKENS=2048
