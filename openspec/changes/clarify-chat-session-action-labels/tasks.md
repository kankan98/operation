## 1. 回归测试

- [x] 1.1 在 `SessionGroupList` 测试中加入未命名会话样例，先复现“新对话”按钮名称冲突。
- [x] 1.2 运行目标测试，确认新增测试在当前实现下失败且失败原因符合预期。

## 2. 前端实现

- [x] 2.1 为会话操作菜单生成独立可访问名称：已命名会话使用“会话操作菜单：<标题>”，未命名会话使用“会话操作菜单：未命名对话”。
- [x] 2.2 更新既有会话菜单测试断言，匹配新的可访问名称格式。

## 3. 验证

- [x] 3.1 运行目标前端测试并确认通过。
- [x] 3.2 运行 `openspec validate clarify-chat-session-action-labels --strict`。
- [x] 3.3 运行前端 lint、完整测试和 build。

## 4. 发布复测

- [x] 4.1 提交并推送本次变更，不包含未跟踪截图文件。
- [x] 4.2 部署到服务器并确认健康检查通过。
- [x] 4.3 用 Playwright 在线上复测按钮可访问名称和主“新对话”点击行为。
