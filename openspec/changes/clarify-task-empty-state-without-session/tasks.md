## 1. 回归测试

- [x] 1.1 在任务面板测试中加入无 `sessionId` 的空态断言，要求显示新对话引导文案且不显示“当前会话还没有创建任务”。
- [x] 1.2 运行目标测试，确认新增测试在当前实现下失败且失败原因符合预期。

## 2. 前端实现

- [x] 2.1 为 `TaskOverviewEmpty` 增加会话上下文参数，默认保持已有会话文案。
- [x] 2.2 在 `TaskPanel` 渲染空态时根据 `sessionId` 传入是否存在当前会话。
- [x] 2.3 补充已有会话无任务的测试断言，确保旧文案保留。

## 3. 验证

- [x] 3.1 运行目标任务面板测试并确认通过。
- [x] 3.2 运行 `openspec validate clarify-task-empty-state-without-session --strict`。
- [x] 3.3 运行前端 lint、完整测试和 build。

## 4. 发布复测

- [ ] 4.1 提交并推送本次变更，不包含未跟踪截图文件。
- [ ] 4.2 部署到服务器并确认健康检查通过。
- [ ] 4.3 用 Playwright 在线上 `/chat` 复测无会话任务空态文案。
