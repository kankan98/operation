## 1. Frontend Display

- [x] 1.1 Render saved `decision.snapshot.capturedAt` in the selected opportunity decision detail with a neutral `快照时间` label.
- [x] 1.2 Keep the existing form footer snapshot hint unchanged.

## 2. Tests

- [x] 2.1 Add selected-detail coverage for the saved snapshot capture time label.
- [x] 2.2 Add source-isolation coverage proving the label uses `snapshot.capturedAt` instead of decision timestamps or current render time.

## 3. Documentation And Validation

- [x] 3.1 Sync the main opportunity research workspace spec and developer/roadmap docs.
- [x] 3.2 Run focused frontend tests, frontend build, OpenSpec validation, and diff checks before archiving.
