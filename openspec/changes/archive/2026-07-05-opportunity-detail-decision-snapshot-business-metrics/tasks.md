## 1. Frontend Display

- [x] 1.1 Render saved `decision.snapshot.businessSignals.metrics` in the selected opportunity decision detail with compact neutral labels.
- [x] 1.2 Skip snapshot business metric labels when saved snapshot metrics are `null` or all display values are missing.

## 2. Tests

- [x] 2.1 Add selected-detail coverage for saved snapshot net margin, ROI, breakeven price, and contribution profit per unit.
- [x] 2.2 Add source-isolation coverage proving current opportunity business metrics are not used when snapshot metrics are `null` or differ.

## 3. Documentation And Validation

- [x] 3.1 Sync the main opportunity research workspace spec and developer/roadmap docs.
- [x] 3.2 Run focused frontend tests, frontend build, OpenSpec validation, and diff checks before archiving.
